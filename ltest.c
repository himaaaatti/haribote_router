#include <stdio.h>
#include <stdbool.h>
#include <linux/if.h>


/*
 * args:
 *   - const char* device   : name of network interface
 *   - bool promisc_flag     : flag of promisc mode.
 *   - bool ip_only          : if flagged, this function read ip packet only.
 *
 *
 */
int init_raw_socket(const char* device, bool promisc_flag, bool ip_only)
{

}

int main(int argc, char const* argv[])
{
    
    return 0;
}

